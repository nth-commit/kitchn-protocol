import { Provider } from '@ethersproject/abstract-provider'

export type Kitchn = {}

/**
 * Initializes the Kitcn SDK.
 *
 * @param _ethersProvider
 * @returns An instance of the Kitchn SDK, authorized by the given provider.
 */
export function initKitchn(_ethersProvider: Provider): Kitchn {
  return {}
}
